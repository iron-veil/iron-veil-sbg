# GitHub Security Features referenced by Veil

- **Code Scanning (CodeQL)** — Default setup enables scanning on PRs and pushes.
- **Secret Scanning & Push Protection** — Blocks committing known secrets to the default branch.
- **Dependabot** — Version updates for GitHub Actions (and other ecosystems if configured).

Enable these in **Settings → Code security & analysis** and **Security → Code scanning alerts**.
