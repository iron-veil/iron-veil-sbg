# GitHub Security Features referenced by Veil

- Code Scanning (CodeQL) — Default setup enables scanning on PRs and pushes.
- Secret Scanning and Push Protection — Prevents committing secrets to default branch.
- Dependabot — Version updates for GitHub Actions in this repository.

Add these in the repository settings under “Code security & analysis” and “Security → Code scanning alerts”.
