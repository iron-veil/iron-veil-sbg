name: Veil — Security Baseline Gate

on:
  pull_request:
  push:
    branches: [ "**" ]

permissions:
  contents: read

jobs:
  sbg:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write  

    steps:
      - name: Harden runner
        uses: step-security/harden-runner@v2
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@v4

      - name: Run SBG
        uses: actions/github-script@v8
        with:
          script: |
            // Your Veil SBG script here (reads repo state and posts summary)
